import OverviewBox from "../../../General/overviewBox/overviewBox";

const AnalyticsOverview = () => {
  // For now, we'll assume OverviewBox handles its own data checking
  // If no data, show empty state
  return <OverviewBox />;
};

export default AnalyticsOverview;
